machine:
  pre:
    - cd /opt/circleci/.pyenv; git pull; cd ~

  timezone:
    Asia/Seoul

  post:
    - pyenv gloabl 3.6.0

dependencies:
  cache_directories:
    - ~/virtualenvs

  pre:
    - pip install -r requirements/ci.txt

test:
  override:
    - flake8 --config .flake8
    - pytest 
