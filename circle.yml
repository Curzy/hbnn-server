machine:
  pre:
    - cd ~/virtualenvs; virtualenv hbnn; source bin/activate;

  timezone:
    Asia/Seoul

  python:
    version: 3.6.0

dependencies:
  cache_directories:
    - ~/virtualenvs

  pre:
    - pip install -r requirements/ci.txt

test:
  override:
    - flake8 --config .flake8
    - pytest 
