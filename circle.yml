machine:
  pre:
    - cd /opt/circleci/.pyenv; git pull; cd ~

  timezone:
    Asia/Seoul

dependencies:
  cache_directories:
    - ~/virtualenvs

  pre:
    - cd ~/virtualenvs; virtualenv hbnn; source bin/activate; cd ~/hbnn; pip install -r requirements/ci.txt

test:
  override:
    - flake8 --config .flake8
    - pytest 
