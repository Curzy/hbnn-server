machine:
  pre:
    - cd /opt/circleci/.pyenv; git pull; cd ~

  timezone:
    Asia/Seoul

  python:
    version: 3.6.0

dependencies:
  pre:
    - pip install -r requirements/ci.txt

test:
  override:
    - flake8 --config .flake8

  post:
    - pytest 
